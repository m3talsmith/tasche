%h2
  Create a New Task for Project
  = @project.id
  
- form_for :task, :url => project_tasks_path(:project_id => @project.id), :html => {:method => :post} do |f|
  = render :partial => "form", :locals => {:f => f}